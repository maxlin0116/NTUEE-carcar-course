# Libraries

This folder is for custom PlatformIO libraries.

- Add a library in its own subfolder.
- Each library should have its own src/ and include/ when needed.
- Example structure:

  lib/
    MyLib/
      src/
        MyLib.cpp
      include/
        MyLib.h

If you do not have custom libraries yet, you can keep this folder empty.
